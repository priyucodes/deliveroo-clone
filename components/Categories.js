import { View, Text, ScrollView } from 'react-native';
const Categories = () => {
  return (
    <ScrollView>
      {/* CategoryCard */}
      <Text></Text>
    </ScrollView>
  );
};
export default Categories;
