import { View, Text } from 'react-native';
const DishRow = ({ id, name, desceription, price, image }) => {
  return (
    <View>
      <Text>DishRow</Text>
    </View>
  );
};
export default DishRow;
